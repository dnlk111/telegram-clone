import { schema } from "./schema";

/**
 * WatermelonDB migrations.
 * Version 1 = initial schema (no migration steps).
 */
export const migrations = [
  // future: addMigration(schema, 2, ({ db }) => ...),
];
